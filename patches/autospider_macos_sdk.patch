272a273,278
> # Patch MacOS SDK
> if platform.system() == "Darwin":
>     macos_sdk_dir = os.environ.get("MACOS_SDK_DIR")
>     if macos_sdk_dir:
>         CONFIGURE_ARGS += ' --with-macos-sdk=' + macos_sdk_dir
>
