description: Equi-join by single attribute of type INT(4).
suite: operators
benchmark: equi_join-single
readonly: yes
pattern: '^Execute query:.*'
configurations:
    Interpreter: --backend Interpreter
label: 'Scale factor'
tables: [ 'Relation' ]
cases:
    0.0:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.0
    0.1:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.1
    0.2:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.2
    0.3:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.3
    0.4:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.4
    0.5:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.5
    0.6:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.6
    0.7:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.7
    0.8:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.8
    0.9:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   0.9
    1.0:
        query: SELECT R.id, S.id FROM Relation R, Relation S WHERE R.id = S.fid;
        tables:
            - name: 'Relation'
              sf:   1.0
compare_to:
    'DuckDB': 'benchmark/operators/equi_join_i32_single_duckdb.sh'
    'PostgreSQL': 'benchmark/operators/equi_join_i32_single_postgresql.sh'
    'HyPer': 'benchmark/operators/equi_join_i32_single_hyper.py'
