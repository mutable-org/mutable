description: An optional test that should fail
db: ours
query: |
    SELECT key FROM R WHERE key = 1;
required: NO

stages:
    lexer:
        returncode: 0

    parser:
        returncode: 0

    sema:
        # Query is valid, so return code should be 0
        returncode: 1

    end2end:
        # Will not be tested because of previous failiure
        returncode: 0
