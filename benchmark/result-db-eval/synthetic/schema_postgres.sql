CREATE TABLE rel_0 (
    id_1 INTEGER NOT NULL,
    id_0 INTEGER NOT NULL,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_1 (
    id INTEGER NOT NULL PRIMARY KEY,
    fk INTEGER NOT NULL,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_7 (
    id INTEGER NOT NULL PRIMARY KEY,
    fk INTEGER NOT NULL,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_2 (
    id INTEGER NOT NULL PRIMARY KEY,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_3 (
    id INTEGER NOT NULL PRIMARY KEY,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_4 (
    id INTEGER NOT NULL PRIMARY KEY,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_5 (
    id INTEGER NOT NULL PRIMARY KEY,
    w CHAR(100) NOT NULL
);
CREATE TABLE rel_6 (
    id INTEGER NOT NULL PRIMARY KEY,
    w CHAR(100) NOT NULL
);