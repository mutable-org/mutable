description: Projection of 1 to 10 attributes of type BOOL.
suite: operators
benchmark: projection
readonly: yes
pattern: '^Execute query:.*'
configurations:
    Interpreter: --backend Interpreter
    WebAssembly: --backend WasmV8
label: 'Number of attributes'
tables: [ 'Attributes_multi_b' ]
cases:
    1: |
        SELECT a0
        FROM Attributes_multi_b;
    2: |
        SELECT a0, a1
        FROM Attributes_multi_b;
    3: |
        SELECT a0, a1, a2
        FROM Attributes_multi_b;
    4: |
        SELECT a0, a1, a2, a3
        FROM Attributes_multi_b;
